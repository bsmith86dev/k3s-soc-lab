---
# ansible/playbooks/01-baseline.yml
# Apply common hardening baseline to all Linux hosts
# Safe to re-run at any time — all tasks are idempotent

- name: Baseline all Linux hosts
  hosts: linux_hosts
  become: true
  gather_facts: true
  roles:
    - common
  tags:
    - baseline
    - common
